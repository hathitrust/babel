                <fieldset>
                  <input id="type"
                       type="radio" name="resource" class="htresource request required" value="type"><b>item type</b></input>
                  <p class="tiny">Select and submit this to determine the item type of your ID if you're unsure whether it corresponds to a volume or an article.</p>
<label for="resource" class="error" style="display:none;">Choose <b>item type</b> to determine resource type or select one of the following resources:</label>
                </fieldset>
